#include "main.h"
/**
 * _strlen - returs the lenth of a string
 * @s: string
 * Return: length
 */
int _strlen(char *s)
{
	int lenth = 0;

	while (*s != '\0')
	{
		length++;
		s++;
	}

	return (length);
}
